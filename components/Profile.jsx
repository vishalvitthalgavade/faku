'use client';
import { useState } from 'react';
export default function Profile({profile,balance,onBack,onSave}){const [name,setName]=useState(profile.name),[upi,setUpi]=useState(profile.upiId),[bal,setBal]=useState(balance);return <section className="screen active"><header className="top-bar"><button className="icon-btn" onClick={onBack}>←</button><div className="title">My Profile (Demo)</div><div style={{width:40}}/></header><div className="form-card"><label>Your name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Priya Sharma"/><label>Your UPI ID</label><input value={upi} onChange={e=>setUpi(e.target.value)} placeholder="e.g. priya@edu"/><label>Reset demo wallet balance</label><input type="number" value={bal} onChange={e=>setBal(e.target.value)}/><button className="primary-btn" onClick={()=>onSave({name:name.trim()||'You',upiId:upi.trim()||'you@edu'},Number(bal))}>Save</button></div></section>}
